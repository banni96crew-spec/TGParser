# Browser Matrix

## Required viewports

| Class | Sizes |
|---|---|
| Narrow mobile | 320×568, 360×800 |
| Standard mobile | 375×812, 390×844 |
| Large mobile | 412×915 |
| Tablet portrait | 768×1024 |
| Intermediate | 820×1180, 1024×768 |
| Desktop | 1280×800, 1440×900 |
| Wide desktop | 1920×1080 |

Also probe widths `319`, `767`, `1023` near breakpoints.

## Minimum stage viewports (hero / section DoD)

`320`, `375`, `768`, `1440`

## Scenario checklist (summary)

Production build → clean storage → load/reload → CTA → interaction states → keyboard → forms → reduced motion → zoom 200% → long content → no horizontal scroll → console/network → a11y → Lighthouse median of 3 runs → screenshot vs principles.

Owner: QA  
Full scenario list lives here; skills reference this file instead of copying.
