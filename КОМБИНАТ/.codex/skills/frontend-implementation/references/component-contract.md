# Component contract

Each component documents:

- name / path
- server vs client
- props (typed)
- states: default, hover, focus, active, disabled, loading, empty, success, error
- a11y: role, label, keyboard
- tokens used
- out-of-scope variations
